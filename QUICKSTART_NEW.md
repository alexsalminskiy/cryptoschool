# 🚀 CRYPTO ACADEMY - Обновлённый Быстрый Старт

## ⚠️ ВАЖНЫЕ ИЗМЕНЕНИЯ

### Новая система доступа:
1. ✅ **Статьи доступны ТОЛЬКО после входа в систему**
2. ✅ **Новые пользователи ждут одобрения админа**
3. ✅ **Админ одобряет доступ через админ-панель**

---

## 📋 Первый запуск - Что нужно сделать

### Шаг 1: Обновите базу данных (1 минута)

1. Откройте **Supabase SQL Editor**
2. Скопируйте и запустите SQL из файла `/app/supabase_add_approval.sql`:

```sql
-- Добавить поле approved в таблицу profiles
ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS approved BOOLEAN DEFAULT false;

ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS approved_at TIMESTAMP WITH TIME ZONE;

ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS approved_by UUID REFERENCES profiles(id);

CREATE INDEX IF NOT EXISTS idx_profiles_approved ON profiles(approved);

-- Обновить существующих админов
UPDATE profiles 
SET approved = true, approved_at = NOW()
WHERE role = 'admin' AND approved = false;
```

3. Нажмите **RUN** ✅

### Шаг 2: Создайте админ аккаунт (2 минуты)

1. **Зарегистрируйтесь**: http://localhost:3000/sign-up
   - Введите email и пароль
   - Нажмите "Регистрация"

2. **Сделайте себя админом в Supabase**:
   - Откройте Supabase Dashboard → **Table Editor** → **profiles**
   - Найдите свою запись (ваш email)
   - Измените:
     - `role`: с `user` на `admin`
     - `approved`: на `true` (галочка)
   - Сохраните изменения

3. **Войдите как админ**: http://localhost:3000/sign-in

---

## 🎯 Как это работает сейчас

### Для обычных пользователей:

1. **Регистрация** (`/sign-up`)
   - Пользователь создаёт аккаунт
   - `approved = false` по умолчанию

2. **Ожидание одобрения** (`/pending-approval`)
   - После входа попадает на страницу ожидания
   - НЕ может просматривать статьи
   - Может проверить статус одобрения

3. **После одобрения админом**
   - Получает полный доступ к платформе
   - Может читать все опубликованные статьи
   - Может использовать поиск и фильтры

### Для админов:

1. **Доступ ко всему**
   - Админы автоматически одобрены
   - Полный доступ к статьям
   - Доступ к админ-панели

2. **Управление пользователями** (`/admin/users`)
   - Просмотр всех пользователей
   - Одобрение новых пользователей (кнопка "Одобрить")
   - Отклонение доступа (кнопка "Отклонить")
   - Статистика: сколько ждут одобрения

3. **Управление статьями** (`/admin/articles`)
   - Создание новых статей
   - Редактирование существующих
   - Удаление статей

---

## 🎨 Новые страницы

### `/pending-approval`
- Красивая страница ожидания
- Информация о статусе
- Кнопка проверки статуса
- Кнопка выхода

### `/admin/users`
- Таблица всех пользователей
- Статистика (всего/ожидают/одобрено/админы)
- Поиск по email
- Фильтры по статусу
- Кнопки одобрения/отклонения

---

## 📊 Обновлённая админ-панель

### Дашборд теперь показывает:
- ✅ Всего статей
- ✅ Опубликовано статей
- ✅ Всего просмотров
- ✅ **Всего пользователей** (новое!)
- ✅ **Ожидают одобрения** (новое! с подсветкой)

### Новое меню админки:
```
📊 Панель управления
📄 Управление статьями  
👥 Управление пользователями (НОВОЕ!)
⬅️ На главную
```

---

## 🔄 Процесс одобрения пользователя

### Сценарий 1: Новый пользователь регистрируется

1. Пользователь заходит на сайт
2. Видит красивый лендинг с призывом зарегистрироваться
3. Нажимает "Зарегистрироваться"
4. Заполняет форму регистрации
5. После входа попадает на `/pending-approval`
6. Видит сообщение об ожидании одобрения

### Сценарий 2: Админ одобряет пользователя

1. Админ заходит в админ-панель
2. Видит на дашборде: "⚠️ 1 пользователь ждёт одобрения"
3. Переходит в "Управление пользователями"
4. Видит список всех пользователей
5. Находит нового пользователя (статус "Ожидает")
6. Нажимает кнопку "Одобрить" ✅
7. Пользователь одобрен!

### Сценарий 3: Пользователь получает доступ

1. Пользователь на странице `/pending-approval`
2. Нажимает "Проверить статус одобрения"
3. Если одобрен → редирект на `/articles`
4. Теперь может читать все статьи!

---

## 🛡️ Защита маршрутов

### Публичные страницы (без авторизации):
- `/` - Главная (лендинг с CTA)
- `/sign-in` - Вход
- `/sign-up` - Регистрация
- `/forgot-password` - Восстановление пароля

### Требуют авторизации:
- `/pending-approval` - Только для неодобренных пользователей
- `/articles` - Требует авторизации + одобрения
- `/articles/[slug]` - Требует авторизации + одобрения

### Только для админов:
- `/admin/*` - Вся админ-панель

---

## 🎯 Тестирование новой системы

### Тест 1: Регистрация и ожидание

```bash
1. Откройте http://localhost:3000
2. Нажмите "Зарегистрироваться"
3. Создайте тестовый аккаунт (test@example.com)
4. Войдите с этим аккаунтом
5. ✅ Должны увидеть страницу "Ожидание одобрения"
6. Попробуйте перейти на /articles
7. ✅ Должны быть возвращены на /pending-approval
```

### Тест 2: Одобрение админом

```bash
1. Войдите как админ
2. Перейдите в Админ-панель
3. Откройте "Управление пользователями"
4. ✅ Должны увидеть тестового пользователя со статусом "Ожидает"
5. Нажмите "Одобрить"
6. ✅ Статус должен измениться на "Одобрен"
```

### Тест 3: Доступ после одобрения

```bash
1. Выйдите из админа
2. Войдите как тестовый пользователь
3. ✅ Должны автоматически попасть на /articles
4. ✅ Можете просматривать статьи
```

---

## 📝 База данных - новые поля

### Таблица `profiles`:
```
id              UUID        - ID пользователя
email           TEXT        - Email
role            TEXT        - 'user' | 'admin'
approved        BOOLEAN     - ✨ НОВОЕ! Одобрен ли пользователь
approved_at     TIMESTAMP   - ✨ НОВОЕ! Когда одобрен
approved_by     UUID        - ✨ НОВОЕ! Кто одобрил
created_at      TIMESTAMP   - Дата регистрации
```

---

## 🐛 Troubleshooting

### Не могу войти на /articles
- ✅ Проверьте, что вы авторизованы
- ✅ Проверьте в Supabase: `approved = true`
- ✅ Если админ, `approved` должен быть `true`

### Админ не видит новых пользователей
- ✅ Обновите страницу /admin/users
- ✅ Проверьте фильтр (должен быть "Все пользователи" или "Ожидают одобрения")

### После одобрения пользователь все еще видит "Ожидание"
- ✅ Пользователь должен нажать "Проверить статус"
- ✅ Или перелогиниться

---

## 🎉 Готово!

Ваша платформа теперь имеет:
- ✅ Защищённый доступ к статьям
- ✅ Систему одобрения пользователей
- ✅ Полноценную админ-панель
- ✅ Управление пользователями

**Начинайте использовать!** 🚀
