# Crypto Academy - Руководство по настройке и использованию

## 🎉 Что уже готово

✅ **Полностью функциональная платформа** с аутентификацией, публичными страницами и админ-панелью
✅ **Supabase интеграция** - база данных и хранилище изображений настроены
✅ **Мультиязычность** - русский, английский, казахский
✅ **Современный UI** - тёмная крипто-тема с неоновыми акцентами
✅ **Markdown редактор** - продвинутый редактор с preview и toolbar
✅ **Адаптивный дизайн** - работает на всех устройствах

## 📋 Структура проекта

```
/app/
├── app/
│   ├── page.js                      # Главная страница
│   ├── layout.js                    # Главный layout
│   ├── globals.css                  # Глобальные стили
│   ├── sign-in/page.js             # Страница входа
│   ├── sign-up/page.js             # Страница регистрации
│   ├── forgot-password/page.js     # Восстановление пароля
│   ├── articles/
│   │   ├── page.js                 # Список всех статей
│   │   └── [slug]/page.js          # Детальная страница статьи
│   ├── admin/
│   │   ├── layout.js               # Админ layout
│   │   ├── page.js                 # Дашборд
│   │   └── articles/
│   │       ├── page.js             # Управление статьями
│   │       ├── new/page.js         # Создание статьи
│   │       └── [id]/page.js        # Редактирование статьи
│   └── api/
│       ├── articles/route.js       # CRUD API для статей
│       ├── articles/[slug]/route.js # Получение статьи по slug
│       └── upload/route.js         # Загрузка изображений
├── components/
│   ├── Header.js                    # Навигация
│   ├── theme-provider.js            # Провайдер темы
│   └── ui/                          # shadcn/ui компоненты
├── contexts/
│   └── AuthContext.js              # Контекст аутентификации
├── lib/
│   ├── supabase.js                 # Supabase клиент
│   ├── i18n.js                     # Переводы
│   └── utils.js                    # Утилиты
└── middleware.js                    # Защита роутов
```

## 🚀 Первый запуск

### 1. Создайте админ аккаунт

1. Перейдите на `/sign-up`
2. Зарегистрируйтесь с email, который вы хотите использовать как админ
3. Войдите в ваш проект Supabase:
   - Откройте **Table Editor** → **profiles**
   - Найдите вашу запись (по email)
   - Измените `role` с `user` на `admin`
   - Сохраните изменения

### 2. Войдите как админ

1. Перейдите на `/sign-in`
2. Войдите с вашими данными
3. Теперь у вас должен появиться раздел "Админ-панель" в навигации

### 3. Создайте первую статью

1. Перейдите в `/admin/articles`
2. Нажмите "Создать статью"
3. Заполните:
   - **Заголовок**: Например "Введение в Bitcoin"
   - **Slug**: автоматически сгенерируется из заголовка
   - **Категория**: Выберите из списка (Bitcoin, Ethereum, DeFi и т.д.)
   - **Обложка**: Загрузите изображение (опционально)
   - **Содержание**: Используйте Markdown редактор
4. Нажмите "Опубликовать" или "Сохранить черновик"

## 📝 Markdown возможности

Редактор поддерживает полный Markdown синтаксис:

```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3

**Жирный текст**
*Курсив*

- Список
- элементов

1. Нумерованный
2. список

[Ссылка](https://example.com)

> Цитата

\`inline код\`

\`\`\`javascript
// Блок кода с подсветкой
const example = 'Hello World';
\`\`\`

| Заголовок 1 | Заголовок 2 |
|-------------|-------------|
| Ячейка 1    | Ячейка 2    |

![Изображение](url-изображения.jpg)
```

## 🎨 Категории статей

Доступные категории:
- **Bitcoin** - всё о Bitcoin
- **Ethereum** - всё об Ethereum
- **DeFi** - децентрализованные финансы
- **Trading** - торговые стратегии
- **Security** - безопасность
- **NFT** - невзаимозаменяемые токены
- **Blockchain Basics** - основы блокчейна
- **Wallets** - криптовалютные кошельки
- **Altcoins** - альтернативные криптовалюты
- **Mining** - майнинг

## 🔐 Роли пользователей

### User (обычный пользователь)
- Просмотр опубликованных статей
- Поиск и фильтрация по категориям
- Регистрация/вход в систему

### Admin (администратор)
- Все права обычного пользователя +
- Создание/редактирование/удаление статей
- Загрузка изображений
- Управление черновиками
- Доступ к статистике (дашборд)

## 📊 API Endpoints

### Публичные

```
GET  /api/articles              # Получить все опубликованные статьи
GET  /api/articles?category=Bitcoin  # Фильтр по категории
GET  /api/articles?search=текст      # Поиск по тексту
GET  /api/articles/[slug]       # Получить статью по slug
```

### Админские (требуют аутентификации и роли admin)

```
POST   /api/articles            # Создать статью
PUT    /api/articles            # Обновить статью
DELETE /api/articles?id=xxx     # Удалить статью
POST   /api/upload              # Загрузить изображение
```

## 🌍 Мультиязычность

Переключение языка доступно через иконку глобуса в header:
- 🇷🇺 Русский (ru)
- 🇬🇧 English (en)
- 🇰🇿 Қазақша (kk)

Для добавления новых переводов отредактируйте `/lib/i18n.js`

## 🎨 Темы

Приложение поддерживает тёмную и светлую темы:
- По умолчанию: **тёмная тема** (crypto-стиль)
- Переключение: кнопка солнца/луны в header
- Автоматическое определение системной темы

## 📁 Supabase Storage

Bucket **article-images** настроен для хранения изображений:
- Публичный доступ на чтение
- Загрузка только для аутентифицированных пользователей
- Автоматическая генерация уникальных имён файлов
- Поддержка всех форматов изображений

## 🔧 Технические детали

### Environment Variables (.env)
```env
NEXT_PUBLIC_SUPABASE_URL=https://xvunmbesvsurmslijlyz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Supabase Tables

**profiles**
- `id` (UUID) - ID пользователя
- `email` (TEXT) - Email
- `role` (TEXT) - Роль: 'user' | 'admin'
- `created_at` (TIMESTAMP) - Дата создания

**articles**
- `id` (UUID) - ID статьи
- `title` (TEXT) - Заголовок
- `slug` (TEXT) - URL-friendly идентификатор
- `content_md` (TEXT) - Markdown контент
- `category` (TEXT) - Категория
- `cover_image_url` (TEXT) - URL обложки
- `status` (TEXT) - Статус: 'draft' | 'published'
- `views` (INTEGER) - Количество просмотров
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления

## 🐛 Troubleshooting

### Не отображается админ-панель
- Убедитесь, что вы вошли в систему
- Проверьте роль в таблице `profiles` (должно быть `admin`)
- Перелогиньтесь после изменения роли

### Ошибка при загрузке изображения
- Проверьте, что bucket `article-images` создан в Supabase Storage
- Убедитесь, что bucket публичный (Public: Yes)
- Проверьте политики доступа в Supabase

### Статьи не отображаются
- Убедитесь, что статус статьи `published` (не `draft`)
- Проверьте Row Level Security политики в Supabase
- Откройте консоль браузера для проверки ошибок API

## 📱 Адаптивность

Приложение полностью адаптивно:
- **Desktop**: Полная навигация, sidebar в админке
- **Tablet**: Адаптивная сетка карточек
- **Mobile**: Hamburger меню, вертикальные карточки

## 🚀 Production Deployment

Для деплоя на production:
1. Обновите `NEXT_PUBLIC_APP_URL` в `.env`
2. Настройте домен в Supabase Auth settings
3. Добавьте домен в Redirect URLs (Supabase)
4. Установите переменные окружения на хостинге

## 💡 Дальнейшее развитие

Возможные улучшения:
- ✨ Комментарии к статьям
- 🔍 Расширенный поиск и фильтры
- 📊 Расширенная аналитика
- 👤 Профили авторов
- 🏷️ Теги для статей
- 📧 Email подписка
- 🌟 Избранные статьи
- 📱 PWA поддержка

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи Supabase (SQL Editor → Logs)
3. Проверьте Network tab в DevTools

---

**Crypto Academy** - современная образовательная платформа по криптовалютам 🚀
